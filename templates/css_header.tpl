/**
 * Authors:         {$gCascader->mInfo.author}
 * Webiste:         {$gCascader->mInfo.website}
 *
 * Scheme Title:    {$gCascader->mInfo.title}
 * Scheme Date:     {$gCascader->mInfo.date}
 * Scheme URL:      {$gCascader->mInfo.permalink}
 *
 * Full list of colors in this scheme:
{foreach from=$gCascader->mInfo.colors item=color}
 *       {$color}
{/foreach}
 */


