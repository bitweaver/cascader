/**
 * Scheme title:    {$gCascader->mTitle}
 * Scheme date:     {$gCascader->mInfo.date}
 * Scheme url:      {$gCascader->mInfo.display_url}
 * Scheme designer: Daily Color Scheme
 *                  dailiycolorscheme.com
 *
 * Full list of colors in this scheme:
{foreach from=$gCascader->mInfo.scheme item=color}
 *       {$color}
{/foreach}
 */


